#include <vector>
#include "building.h"

void Building::build(std::vector<face_t> &faces){

}

void Building::build_face(std::vector<face_t> &faces, int face) {
    /*
    1 = towards +x
    2 = towards +z
    3 = towards -x
    4 = towards -z
    */

    // make border via margin

    // for num floors, build floors with num_windows number of windows per floor
}
